@* ======================== _Layout.cshtml ======================== *@
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - FurnitureShop</title>

    <!-- Bootstrap & Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" />

    <!-- ✅ Thêm dòng cần thiết đảm bảo icon ❤️ hiển thị đẹp nhất -->
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body>
    <!-- 🌐 Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top shadow-sm">
        <div class="container">
            <!-- Logo -->
            <a class="navbar-brand fw-bold text-uppercase" asp-controller="Home" asp-action="Index">
                <i class="bi bi-shop"></i> FurnitureShop
            </a>

            <!-- Toggle mobile -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="mainNavbar">
                <!-- Menu trái -->
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString()=="Home"?"active":"")"
                           asp-controller="Home" asp-action="Index">
                            <i class="bi bi-house-door"></i> Trang chủ
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString()=="Products"?"active":"")"
                           asp-controller="Products" asp-action="Index">
                            <i class="bi bi-box-seam"></i> Sản phẩm
                        </a>
                    </li>

                    @* 💖 Thêm menu Yêu thích (mới thêm) *@
                    @if (User.Identity?.IsAuthenticated ?? false && User.IsInRole("User"))
                    {
                        <li class="nav-item">
                            <a class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString()=="Wishlist"?"active":"")"
                               asp-controller="Wishlist" asp-action="Index">
                                <i class="bi bi-heart-fill text-danger"></i> Yêu thích
                            </a>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString()=="Orders"?"active":"")"
                               asp-controller="Orders" asp-action="Index">
                                <i class="bi bi-cart-check"></i> Đơn hàng
                            </a>
                        </li>
                    }
                </ul>

                <!-- Menu phải -->
                <ul class="navbar-nav align-items-center">
                    <!-- 🌗 Nút đổi giao diện -->
                    <li class="nav-item">
                        <button id="toggleTheme" class="btn btn-outline-light btn-sm ms-2">🌙 Dark</button>
                    </li>

                    @if (User.Identity?.IsAuthenticated ?? false)
                    {
                        <li class="nav-item">
                            <span class="navbar-text text-white ms-3 me-2">
                                <i class="bi bi-person-circle"></i> Xin chào, <strong>@User.Identity!.Name</strong>
                            </span>
                        </li>

                        @* Menu ADMIN *@
                        @if (User.IsInRole("Admin"))
                        {
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle text-white" href="#" data-bs-toggle="dropdown">
                                    <i class="bi bi-gear-fill"></i> Quản trị
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end shadow-sm">
                                    <li>
                                        <a class="dropdown-item" asp-controller="AdminProducts" asp-action="Index">
                                            <i class="bi bi-box"></i> Quản lý sản phẩm
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" asp-controller="AdminUsers" asp-action="Index">
                                            <i class="bi bi-people"></i> Quản lý người dùng
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" asp-controller="Coupons" asp-action="Index">
                                            <i class="bi bi-ticket-perforated"></i> Quản lý Coupon
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" asp-controller="Orders" asp-action="AdminIndex">
                                            <i class="bi bi-bag-check"></i> Quản lý đơn hàng
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" asp-controller="Admin" asp-action="Dashboard">
                                            <i class="bi bi-bar-chart-line"></i> Thống kê & biểu đồ
                                        </a>
                                    </li>
                                </ul>
                            </li>
                        }

                        <!-- Đăng xuất -->
                        <li class="nav-item ms-2">
                            <form asp-controller="Account" asp-action="Logout" method="post" class="d-inline">
                                @Html.AntiForgeryToken()
                                <button class="btn btn-sm btn-outline-light">
                                    <i class="bi bi-box-arrow-right"></i> Đăng xuất
                                </button>
                            </form>
                        </li>
                    }
                    else
                    {
                        <!-- Chưa đăng nhập -->
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Account" asp-action="Login">
                                <i class="bi bi-box-arrow-in-right"></i> Đăng nhập
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Account" asp-action="Register">
                                <i class="bi bi-person-plus"></i> Đăng ký
                            </a>
                        </li>
                    }
                </ul>
            </div>
        </div>
    </nav>

    <!-- 💎 Nội dung chính -->
    <main style="padding-top:90px">
        <div class="container">
            @RenderBody()
        </div>
    </main>

    <!-- ⚙️ Footer -->
    <footer class="bg-dark text-light text-center py-4 mt-5 shadow-sm">
        <div class="container">
            <p class="mb-1">© @DateTime.Now.Year - <strong>FurnitureShop</strong>. All rights reserved.</p>
            <p class="small mb-0">
                <i class="bi bi-geo-alt"></i> Lê Trọng Tấn, Hà Đông |
                <i class="bi bi-envelope"></i> support@furnitureshop.vn |
                <i class="bi bi-telephone"></i> 0123 456 789
            </p>
        </div>
    </footer>

    <!-- Script -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    @await RenderSectionAsync("Scripts", required: false)

    <!-- ✅ Toast thông báo -->
    <div class="toast-container position-fixed bottom-0 end-0 p-3">
        @if (TempData["success"] != null)
        {
            <div class="toast align-items-center text-bg-success border-0 show" role="alert">
                <div class="d-flex">
                    <div class="toast-body fw-semibold">@TempData["success"]</div>
                    <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast"></button>
                </div>
            </div>
        }
        @if (TempData["error"] != null)
        {
            <div class="toast align-items-center text-bg-danger border-0 show" role="alert">
                <div class="d-flex">
                    <div class="toast-body fw-semibold">@TempData["error"]</div>
                    <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast"></button>
                </div>
            </div>
        }
    </div>

    <!-- 🌙 JS cho Dark mode + Toast -->
    <script>
        // Hiển thị Toast tự động
        document.querySelectorAll('.toast').forEach(el => {
            const toast = new bootstrap.Toast(el, { delay: 3000 });
            toast.show();
        });

        // Dark mode toggle
        const toggleBtn = document.getElementById('toggleTheme');
        const body = document.body;

        toggleBtn.addEventListener('click', () => {
            body.classList.toggle('dark-mode');
            const mode = body.classList.contains('dark-mode') ? 'dark' : 'light';
            localStorage.setItem('theme', mode);
            toggleBtn.innerText = mode === 'dark' ? '☀️ Light' : '🌙 Dark';
        });

        window.onload = () => {
            const saved = localStorage.getItem('theme');
            if (saved === 'dark') {
                body.classList.add('dark-mode');
                toggleBtn.innerText = '☀️ Light';
            }
        };
    </script>

    <style>
        /* 🌗 Dark mode giao diện */
        .dark-mode {
            background-color: #121212 !important;
            color: #eaeaea !important;
        }

            .dark-mode .navbar, .dark-mode footer {
                background-color: #1f1f1f !important;
                color: #fff !important;
            }

            .dark-mode .card {
                background-color: #1e1e1e;
                color: #fff;
                border: 1px solid #333;
            }

            .dark-mode .btn-outline-primary {
                color: #80bfff;
                border-color: #80bfff;
            }

                .dark-mode .btn-outline-primary:hover {
                    background-color: #80bfff;
                    color: #000;
                }

            .dark-mode .form-control, .dark-mode .form-select {
                background-color: #2c2c2c;
                color: #eee;
                border: 1px solid #444;
            }

            .dark-mode .dropdown-menu {
                background-color: #222;
                color: #eee;
            }

        .navbar .nav-link:hover {
            text-decoration: underline;
        }
    </style>
</body>
</html>
